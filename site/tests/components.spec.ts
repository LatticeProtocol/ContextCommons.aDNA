// VignetteCard tests removed in M08-C L8 — vignettes were merged into the
// "Where we're working" PilotCard section on the homepage. The VignetteCard
// component is unused; the test file is preserved as a placeholder for future
// shared-component coverage.

import { test } from '@playwright/test';

test.describe.skip('VignetteCard components on home page (removed in M08-C L8)', () => {
  test('placeholder', () => {});
});
